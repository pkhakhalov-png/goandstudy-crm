-- Ориентировочный месяц получения оффера / зачисления студента.
-- Формат 'YYYY-MM' (текст), NULL = не задано. Куратор, продажник и админ могут
-- проставлять это значение на карточке клиента. Используется отдельной вкладкой
-- аналитики «Выплаты кураторам» для прогноза, в каком месяце придётся выплачивать
-- второй этап (остаток 25 000 ₽) какому куратору. Действующие RPC/триггеры и суммы
-- расходов не затрагиваются.

ALTER TABLE public.clients
  ADD COLUMN IF NOT EXISTS expected_offer_month TEXT;
