# Обращения к базе: что было, что стало

Замеры сделаны с ноутбука против боевой базы, лучшее из трёх прогонов,
12–13 сентября 2026. Повторить: `npx tsx scripts/perf-audit.ts`.

## Сначала — чем измеряли и почему цифрам можно верить

**Один поход до базы с этого ноутбука стоит около 107 мс.** Это проверено
отдельно (`scripts/perf-probe.ts`): пустой запрос «отдай одну строку» идёт
столько же, сколько запрос на тысячу строк. Отсюда главный вывод всей работы:

> Экраны тормозили не из-за плохих запросов, а из-за их количества.
> Двадцать восемь обращений подряд нельзя уложить в 700 мс никакими индексами.

Поэтому в таблицах ниже рядом со временем стоит **число обращений** — оно не
зависит от того, откуда меряли. На Vercel, где приложение стоит рядом с базой,
время будет меньше, а число обращений останется тем же.

**Почему замер сервисным ключом честен.** Экраны читают под пользователем,
через RLS. Политики (`supabase/migrations/20260410000000_init.sql`) —
`Authenticated read USING (true)`: предиката нет, лишнего прохода по таблице он
не добавляет, план запроса тот же. Если политики станут условными, эта оговорка
перестанет быть верной.

**Что не входит в замер.** Проверка входа (`auth.getUser()`) — отдельный поход
в Auth API, одинаковый для всех экранов. И отрисовка React: на этих экранах это
сборка HTML из готовых массивов, десятки миллисекунд.

## Экраны админки

Норма — 700 мс. Уложились все.

| Экран | Было | Стало | Обращений | Что сделано |
|---|---:|---:|---:|---|
| `/admin` | 226 мс | **195 мс** | 6 | не трогали — уже читал одной пачкой |
| `/admin/clients` | 215 мс | **196 мс** | 9 | не трогали |
| `/admin/payments` | 189 мс | **187 мс** | 7 | не трогали |
| `/admin/expenses` | 186 мс | **185 мс** | 5 | не трогали |
| `/admin/funnel` | 752 мс | **440 мс** | 35 → 9 | сделки читаются один раз и раскладываются по этапам в памяти вместо двух запросов на этап; устранена обрезка |
| `/admin/sales` | 190 мс | **189 мс** | 6 | не трогали |
| `/admin/analytics` | 272 мс | **286 мс** | 4 | не трогали |
| `/admin/invoices` | 179 мс | **196 мс** | 3 | не трогали |
| `/admin/calendar` | 185 мс | **189 мс** | 6 | не трогали |
| `/admin/curators` | 179 мс | **179 мс** | 4 | не трогали |
| `/admin/settings` | 261 мс | **255 мс** | 6 | не трогали |
| `/admin/seo` | 535 мс | **133 мс** | 24 → 8 | итоги по страницам считает воркер после импорта, экран читает готовое |
| `/admin/seo/articles` | 678 мс | **518 мс** | 8 | из `meta` берутся только картинки вместо всего поля; шесть выборок шли цепочкой — теперь одной пачкой |
| `/admin/seo/pages` | 488 мс | **102 мс** | 22 → 6 | тот же снимок итогов |
| `/admin/seo/effect` | 1588 мс | **377 мс** | 28 → 10 | убран запрос на каждую статью (было по три), окна считаются одним проходом |
| `/admin/seo/indexation` · Google | 1361 мс | **386 мс** | 26 → 10 | то же плюс отбор только нужных полей `meta` |
| `/admin/seo/indexation` · Яндекс | 1361 мс | **91 мс** | 26 → 1 | вкладка поднимала всю гугловую часть и выбрасывала её |
| `/admin/seo/positions` · Google | 92 мс | **90 мс** | 1 | уже читал готовый снимок |
| `/admin/seo/positions` · Яндекс | 95 мс | **95 мс** | 2 | то же |
| `/admin/seo/topics` | 96 мс | **97 мс** | 1 | не трогали |
| `/admin/seo/clusters` | 95 мс | **98 мс** | 1 | не трогали |
| `/admin/seo/findings` | 173 мс | **164 мс** | 1 | не трогали |
| `/admin/seo/opportunities` | 95 мс | **89 мс** | 1 | не трогали |
| `/admin/seo/schema` | 180 мс | **182 мс** | 2 | не трогали |
| `/admin/seo/experiments` | 89 мс | **87 мс** | 1 | не трогали |

Ни один отдельный запрос на этих экранах не превышает 200 мс.

## Экраны РОПа и продажника

В список задания они не входили, но именно здесь нашлось худшее — и это не про
скорость. Подробности в разделе «Где цифры врали».

| Экран | Было | Стало | Обращений | Что изменилось |
|---|---:|---:|---:|---|
| `/rop` | 554 мс | 1262 мс | 8 → 33 | читал 1000 сообщений из 23 070 — стало читать все |
| `/rop/analytics` | 369 мс | **448 мс** | 4 → 7 | читал 1000 смен этапа из 1106 |
| `/rop/response-times` | 314 мс | 1230 мс | 5 → 30 | читал 1000 самых старых сообщений |
| `/rop/conversions` | 351 мс | 1053 мс | 5 → 31 | то же |

Колонка «было» здесь измерена отдельно (`scripts/perf-rop-before.ts`): эти экраны
я добавил в замер уже после правки, и брать числа из головы было бы нечестно.

Эти четыре стали медленнее, и это осознанный размен: **раньше они были быстрыми
потому, что читали малую часть данных и молчали об этом.** Что с этим делать
дальше — в разделе «Что осталось нерешённым».

## Шаги фонового воркера

Бюджет шага — 30 секунд. Укладываются все.

| Шаг | Было | Стало | Обращений | Что сделано |
|---|---:|---:|---:|---|
| `positions_snapshot` | 3167 мс | 3100 мс | 44 | добавлен порядок строк при постраничном чтении (см. ниже) |
| `article_autostart` | 4581 мс | 4975 мс | 140 | счёт строк теперь с тем же условием, что и чтение: 194 обращения → 140 |
| `topics_from_gsc` | 4693 мс | 5346 мс | 140 | то же |
| `index_check_site` | 192 мс | **192 мс** | 2 | не трогали |
| `attribution_stitch` | 239 мс | **95 мс** | 2 → 1 | обновления пачками вместо по одному, проверка ошибок |
| `alerts_check` | 93 мс | **103 мс** | 2 | не трогали |
| `gsc_import` (чтение) | 213 мс | **204 мс** | 2 | добавлен пересчёт снимка итогов после записи |
| `article_update_plan` | 399 мс | **460 мс** | 20 | не трогали |
| `article_index_check` | не замерялось | **422 мс** | 14 → 2 | убран запрос версии на каждую статью |
| `article_autopublish` (отбор) | не замерялось | **526 мс** | по запросу на кандидата → 2 | то же |
| `yandex_sync` | не замерялось | — | по запросу на статью → 2 | то же; время не мерил: шаг ходит в Вебмастер Яндекса, а не только в базу |
| `article_brief`, `article_draft`, `article_qa`, `article_cover`, `article_linkplan`, `article_publish_blog`, `article_verify`, `article_fix` | не замерялось | **344 мс** | 2 | к базе обращаются одинаково: читают статью и её версию, пишут версию. Замерен общий образец, а не каждый по отдельности: время этих шагов определяет модель и сеть, а не база |

`positions_snapshot`, `article_autostart` и `topics_from_gsc` стали на 200–600 мс
медленнее — цена упорядоченного чтения. Почему это правильный размен, ниже.

## Где цифры врали

### Воронка показывала 1000 сделок из 1046

Обычный `select` отдаёт максимум тысячу строк и не считает это ошибкой. Сорока
шести сделок на экране просто не было.

### Экраны РОПа считали по 4% переписки

| Что читалось | Доходило | На самом деле | Потеря |
|---|---:|---:|---:|
| сообщения по сделкам | 1 000 | 23 070 | 96% |
| сделки | 1 000 | 1 047 | 4% |
| смены этапа | 1 000 | 1 106 | 10% |

Экран «Время ответа» считал среднее по переписке **с 12 по 21 апреля** вместо
пяти месяцев, и видел 84 сделки с перепиской вместо 728. Раздел «кому не
ответили» смотрел на последнее сообщение сделки — а последним для него было
апрельское. Цифры на этом экране были не приблизительными, а неверными.

Сейчас читается всё. Числа на этих экранах изменятся — **верны новые**.

### Карточка сделки показывала не всю переписку

У самой длинной сделки в базе **1779 сообщений и 1780 записей в истории**. Её
карточка читала первую тысячу и обрывала остальное. Это не гипотеза про рост —
это уже происходило. Проверить порог: `npx tsx scripts/perf-maxrows-check.ts`.

### Постраничное чтение без заданного порядка

`loadPageDays`, `loadQueryDays` и чтение в `cannibal.ts` брали данные страницами
по тысяче строк, не задав порядок. Postgres не обещает одинаковую
последовательность строк между запросами: соседние страницы могут перекрыться
или разойтись, и часть строк теряется, а часть удваивается — молча. На сорока
страницах подряд это вопрос времени, а не «если». Добавлена сортировка по
первичному ключу; сверка (`scripts/perf-verify.ts`) подтверждает, что дублей нет
и суммы совпадают со старым чтением.

### Счёт строк не совпадал с условием чтения

Те же функции считали число страниц выдачи по **всей** таблице, а читали с
отбором по дате. При выборке за 90 дней из `gsc_daily` это давало 194 обращения
вместо 140, и полсотни из них возвращали пустоту.

### Записи, о неудаче которых никто не узнавал

Supabase не бросает исключение: при неудаче возвращает `{ error }`. Если его не
прочитать, код идёт дальше и отчитывается об успехе. В конвейере таких записей
было **63** — включая смену статуса статьи на «опубликована» и сохранение
снимков. Все они теперь проверяют результат.

### Сколько мест исправлено

| | Было | Стало |
|---|---:|---:|
| выборок без ограничения по растущим таблицам | 79 | **45** |
| из них в приложении, а не в разовых скриптах | 41 | **0** |
| записей без проверки результата в конвейере | 63 | **0** |
| записей без проверки результата всего | 182 | 122 |

Оставшиеся 45 проверены поимённо и ограничены по существу: выборки по одной
сделке (в задачах максимум 68 строк), по списку идентификаторов, по таблицам в
сотни строк (`bookings` — 248, `pages` — 220). Список выдаёт
`npx tsx scripts/perf-truncation.ts`.

## Проверка, что цифры не поехали

`npx tsx scripts/perf-verify.ts` считает то же самое старым и новым способом и
сравнивает поэлементно. Сейчас проходит без расхождений:

- чтение `gsc_page_daily`: строки, суммы кликов и показов, отсутствие дублей;
- «Эффект»: список блога с цифрами, наши статьи, медиана, страницы без показов;
- «Индексация»: состояние каждой статьи, таблица страниц целиком, сводные карточки;
- воронка: число сделок по этапам и какие карточки показаны;
- итоги по страницам: снимок против живого расчёта, по каждому адресу.

Две вещи сравниваются с оговоркой, и обе — свойства арифметики, а не данных:
дробная часть средней позиции складывается из тысяч слагаемых, и порядок
сложения меняет пятнадцатый знак (сравниваем до четвёртого, на экране виден
один); порядок ключей в объекте не считается расхождением.

## Инструменты

| Скрипт | Зачем |
|---|---|
| `scripts/perf-audit.ts` | замер экранов и шагов воркера; `--diff` сравнивает с прошлым, `--verbose` показывает каждый запрос |
| `scripts/perf-verify.ts` | сверка цифр до и после |
| `scripts/perf-probe.ts` | сколько стоит поход до базы и сколько — данные |
| `scripts/perf-truncation.ts` | где выдача может обрезаться молча |
| `scripts/perf-writes.ts` | записи, у которых не проверяется результат |
| `scripts/seo-traffic-snapshot.ts` | пересчитать итоги по страницам вручную |

Замеры лежат рядом: `before-screens.json`, `after-screens.json`, `after-steps.json`.

## Что осталось нерешённым

**1. Три экрана РОПа открываются дольше секунды.** Причина не в запросах, а в
потолке сервера: PostgREST отдаёт максимум тысячу строк за раз (проверено —
просьба о 25 тысячах всё равно даёт тысячу). Двадцать три тысячи сообщений — это
23 обращения, и обойти это из кода нельзя.

Что может помочь, в порядке предпочтения:

- **Представление в базе.** `deal_message_stats` с готовыми числами по каждой
  сделке — 1046 строк вместо 23 000, одно обращение. Это правильное решение, но
  оно требует DDL, а у меня к нему доступа нет, и непроверенный SQL выдавать за
  готовое я не стану.
- **Поднять «Max rows» в настройках Supabase** (API → Max rows) с 1000 до,
  скажем, 10 000. Одна галочка, кода не меняет, режет 23 обращения до 3. Обратная
  сторона: любой случайный `select` без ограничения сможет вытащить десять тысяч
  строк вместо тысячи.
- **Сузить окно.** Считать время ответа не по всей истории, а за последние 90
  дней. Это меняет смысл показателя, поэтому решать вам, а не мне.

**2. `topics_from_gsc` и `article_autostart` читают 135 тысяч строк, около 5
секунд.** В бюджет тридцати секунд укладываются с запасом, но `article_autostart`
запускается раз в час. Подготовлен файл с индексами:
`supabase/migrations/20260913000000_perf_indexes.sql` — **он не применён**,
примените вручную в SQL-редакторе и повторите замер.

**3. Записи без проверки результата за пределами конвейера — 119 штук.**
В конвейере (`lib/seo`, `app/api/seo`) исправлены все 63. Остальные — в действиях
CRM: воронка (23), телеграм-вебхук (12), настройки (10), расходы (9). Я их не
трогал намеренно: сейчас неудачная запись там проходит молча, а проверка
означала бы показ ошибки пользователю. Это смена поведения интерфейса в
полусотне мест, и это ваше решение, а не моё. Список выдаёт
`npx tsx scripts/perf-writes.ts`.

**4. `/admin/seo/articles` — 518 мс.** В норму укладывается, но половина времени уходит
на чтение `qa_report` тринадцати версий. Отчёт нужен целиком: экран считает по
нему проваленные проверки. Ускорить можно, только вынеся счётчики в отдельные
колонки — то есть снова DDL.
